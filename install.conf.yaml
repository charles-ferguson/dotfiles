- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.inputrc: inputrc
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc
    ~/bin_prompt.sh: bin_prompt.sh

    ~/.vimrc: vim/vimrc
    ~/.vim/autoload/plug.vim: vim/vim-plug/plug.vim


    ~/fzf.d: fzf.d/

    ~/.config/gh/config.yml: config_files/config/gh/config.yml

    ~/.docker/config.json: config_files/docker_config.json

    ~/.pryrc: pryrc
    ~/.gemrc: gemrc
